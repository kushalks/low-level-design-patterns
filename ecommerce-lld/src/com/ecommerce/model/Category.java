package com.ecommerce.model;

/**
 * Product categories. An enum keeps the catalog type-safe and easy to filter on.
 */
public enum Category {
    ELECTRONICS,
    FASHION,
    BOOKS,
    GROCERY,
    HOME,
    TOYS
}
